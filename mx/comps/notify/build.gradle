plugins {
  id 'com.jfrog.bintray' version '1.8.0'
}

apply from: '../../../dependencies.gradle'

apply from: '../../../allprojects.gradle'

project(':mx-notify-server') {
  version = '1.3.1'
  description = 'Notification service based on websocket & restful.'

  dependencies {
    compile(
      libs.'mx-utils',
      libs.'mx-service-utils'
    )
    testCompile(
      libs.'spring-test',
      libs.'httpclient'
    )
  }
}

apply from: '../../../subproject.gradle'
