file.simple.root=./data
# 是否以哈希方式生成两层目录来存放文件，可以解决文件名编码规则问题。
file.simple.hash=true
# 哈希目录层数，默认为3层
file.simple.hash.levels=3
# 每个目录中存放文件个数，默认为1000
file.simple.hash.numPerLevel=1000
# 是否在碰到异常后直接关闭文件
file.simple.error.close=true